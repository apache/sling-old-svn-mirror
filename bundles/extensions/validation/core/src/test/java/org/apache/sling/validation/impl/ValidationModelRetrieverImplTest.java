package org.apache.sling.validation.impl;

public class ValidationModelRetrieverImplTest {

    // TODO: check trie mechanism that always the right model (best match) is retrieved
    // check that caching works as expected (even for resource types which have no model!)
}
